# Development V1
Dùng npm. `npm run dev` chạy local; `npm run lint`, `npm test`, `npm run build` là quality gate. Sao chép `.env.example` sang `.env`; không commit credentials. Chưa triển khai scraper, AI thật, auth, payment, export production hoặc social integrations trong Phase 1.
